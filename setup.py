from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='It is the project to predict the wafer is correct using sensor signals.',
    author='Gourav_Chouhan',
    license='MIT',
)
